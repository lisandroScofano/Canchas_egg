spring:
  datasource: 
    url: jdbc:mysql://localhost:3306/cancha?useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update    
  thymeleaf:
    cache: false 
  banner:
    location: classpath:banner.txt
server:
  error:
    whitelabel:
      enabled: false
    